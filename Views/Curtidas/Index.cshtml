@model IEnumerable<projetoRedeSocial.Models.Curtidas>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.curtidaUsuario)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.postCurtida)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.curtidaUsuario.usuarioId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.postCurtida.postId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.idCurtida">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.idCurtida">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.idCurtida">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
