@model IEnumerable<projetoRedeSocial.Models.Bloqueados>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.bloqueioUsuario)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.bloqueadoUsuario)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.bloqueioUsuario.usuarioId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.bloqueadoUsuario.usuarioId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.idBloqueio">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.idBloqueio">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.idBloqueio">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
